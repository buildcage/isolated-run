# Reference

This page holds no reference material of its own. It all lives in the [README](../README.md), and
what follows is a set of links into it.

- [Usage](../README.md#usage): the audit-to-restrict flow, and what a step is made of
- [Inputs](../README.md#inputs): every input, with its default and the engines it applies to
- [Operation modes](../README.md#operation-modes): what `audit` and `restrict` each do
- [Rule syntax](../README.md#rule-syntax): URL rules, host rules, wildcards, ports, regular expressions
- [Engines](../README.md#engines): what `inspect` and `universal` each enforce on
- [CA trust and compatibility](../README.md#ca-trust-and-compatibility): the CA variables `inspect` sets, and what it cannot work with
- [The report](../README.md#the-report): the Job Summary, and the traffic artifact's JSON
- [Passing values to `run`](../README.md#passing-values-to-run): using the step's own `env:`
- [Filesystem access](../README.md#filesystem-access): what is writable, and how to add to it
