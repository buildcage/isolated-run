name: Build and Push Docker Image
run-name: Build and Push Docker Image (${{ github.ref_name }})

on:
  push:
    tags:
      - "v*.*.*"

  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to build (leave empty to use latest)"
        required: false
        type: string

concurrency:
  group: docker-publish
  cancel-in-progress: false

permissions: {}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - engine: universal
            dockerfile: docker/universal/Dockerfile
          - engine: inspect
            dockerfile: docker/inspect/Dockerfile
    outputs:
      tag: ${{ steps.version.outputs.tag }}
      # Matrix legs sharing one output name would clobber each other (last
      # completed leg wins); each leg instead writes to a step-output key
      # suffixed with its own matrix.engine value, so neither collides and
      # both survive into this job's own (statically-declared) outputs.
      digest-universal: ${{ steps.digest.outputs['universal'] }}
      digest-inspect: ${{ steps.digest.outputs['inspect'] }}

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Determine version
        id: version
        env:
          REF_NAME: ${{ github.ref_name }}
          EVENT_NAME: ${{ github.event_name }}
          INPUT_TAG: ${{ inputs.tag }}
        run: |
          if [ "$EVENT_NAME" = "workflow_dispatch" ]; then
            if [ -n "$INPUT_TAG" ]; then
              TAG="$INPUT_TAG"
            else
              TAG=$(git tag --sort=-version:refname --list 'v*.*' | grep -vE '^v[0-9]+\.[0-9]+\.[0-9]+-' | head -1)
              if [ -z "$TAG" ]; then
                echo "::error::No version tag found"
                exit 1
              fi
            fi
            git checkout "$TAG"
          else
            TAG="$REF_NAME"
          fi
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@1f40c72289eff860ee54a304f1438e3cff362e0a # v4.3.0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4.3.0

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302 # v6.2.0
        with:
          images: ghcr.io/${{ github.repository }}
          # Every engine publishes under its own -<engine> suffix, so no image
          # tag is ever engine-ambiguous.
          # "latest" is applied later, by update-major-tag.yml on release publish.
          flavor: latest=false
          tags: |
            type=semver,pattern={{version}},value=${{ steps.version.outputs.tag }},suffix=-${{ matrix.engine }}
            type=raw,value=sha-${{ steps.version.outputs.sha }}-${{ matrix.engine }},priority=100

      - name: Login to GHCR
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        id: build
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        env:
          DOCKER_BUILD_CHECKS_ANNOTATIONS: true
          DOCKER_BUILD_SUMMARY: true
          DOCKER_BUILD_RECORD_UPLOAD: false
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

      - name: Record the digest under this engine's own key
        id: digest
        env:
          DIGEST: ${{ steps.build.outputs.digest }}
          ENGINE: ${{ matrix.engine }}
        run: echo "${ENGINE}=${DIGEST}" >> "$GITHUB_OUTPUT"

  # Gates signing on the pushed image actually running. A release build resolves
  # its packages and base image again, so a green PR says nothing about what this
  # tag carries; signing only what has run keeps a broken image unusable, since
  # the action refuses an unsigned one.
  #
  # Real hosts, not the fixture origin: trusting its self-signed certificate takes
  # a BUILDCAGE_TEST_HOOKS build, which a released image deliberately is not.
  verify-image:
    needs: build-and-push
    permissions:
      contents: read
      packages: read
    strategy:
      fail-fast: false
      matrix:
        # Natively on both: QEMU would put the s6 tree and iptables through a
        # layer the released image never runs under.
        runner: [ubuntu-latest, ubuntu-24.04-arm]
        engine: [universal, inspect]
    runs-on: ${{ matrix.runner }}
    name: "verify (${{ matrix.engine }}, ${{ matrix.runner }})"

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # workflow_dispatch can name a tag other than this run's own ref.
          ref: ${{ needs.build-and-push.outputs.tag }}
          persist-credentials: false

      - uses: ./.github/actions/setup-vp

      - name: Login to GHCR
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Resolve this engine's image ref
        id: image
        env:
          OUTPUTS: ${{ toJSON(needs.build-and-push.outputs) }}
          ENGINE: ${{ matrix.engine }}
          REPO: ${{ github.repository }}
        run: |
          DIGEST=$(jq -er --arg e "$ENGINE" '.["digest-" + $e]' <<< "$OUTPUTS")
          echo "ref=ghcr.io/${REPO}@${DIGEST}" >> "$GITHUB_OUTPUT"

      # The override starts the proxy with --pull never, so the image has to be
      # here first.
      - name: Pull the image that was just pushed
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
        run: docker pull "$IMAGE_REF"

      # The image is not signed yet, so the action's provenance path cannot
      # resolve it and this hook is the only way to name a ref. Never committed.
      - name: Rebuild the action with the local-image test hook enabled
        env:
          BUILDCAGE_BUILD_TEST_HOOKS: "1"
        run: vp run build

      - name: Run an isolated command via the real action
        uses: ./
        env:
          BUILDCAGE_LOCAL_IMAGE_REF: ${{ steps.image.outputs.ref }}
          BUILDCAGE_RUN_DEBUG_SUMMARY_FILE: ${{ runner.temp }}/verify-summary.md
        with:
          proxy_engine: ${{ matrix.engine }}
          # Each engine gets the rule kind it actually enforces on.
          allowed_https_rules: ${{ matrix.engine == 'universal' && 'example.com:443' || '' }}
          allowed_url_rules: ${{ matrix.engine == 'inspect' && 'GET|HEAD https://example.com/**' || '' }}
          # The refusal below is the point, not a failure.
          fail_on_blocked: false
          run: |
            echo "=== an allowed host must be reachable ==="
            # Retried because a release should not go unsigned over one bad
            # minute at the origin. No --insecure: under inspect the proxy
            # terminates TLS, so this also proves the sandbox got its CA.
            curl -fsS --max-time 15 --retry 3 --retry-delay 5 --retry-all-errors \
              https://example.com/ > /dev/null

            echo "=== a host outside the rules must not be ==="
            if curl -fsS --max-time 15 https://neverssl.com/ > /dev/null 2>&1; then
              echo "neverssl.com was reachable"
              exit 1
            fi

      # The engines refuse differently (a rejected connection, or a 403), so the
      # command above only sees that the second request failed. The report says
      # the proxy is why.
      - name: Verify the report recorded both outcomes
        env:
          SUMMARY: ${{ runner.temp }}/verify-summary.md
        run: |
          for HOST in example.com:443 neverssl.com:443; do
            if ! grep -qF "$HOST" "$SUMMARY"; then
              echo "::error::$HOST is missing from the report"
              exit 1
            fi
          done

      # No cleanup step: the action stops its own proxy container in its
      # finally block.

  sign-and-attest:
    needs: [build-and-push, verify-image]
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
      packages: write
      attestations: write
      artifact-metadata: write
    strategy:
      fail-fast: false
      matrix:
        engine: [universal, inspect]
    name: "sign (${{ matrix.engine }})"
    outputs:
      # Same reason as build-and-push's own per-engine output keys above.
      bundle-content-universal: ${{ steps.bundle.outputs['content-universal'] }}
      bundle-content-inspect: ${{ steps.bundle.outputs['content-inspect'] }}

    steps:
      - name: Install cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2

      - name: Login to GHCR
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Resolve this engine's digest
        id: digest
        env:
          OUTPUTS: ${{ toJSON(needs.build-and-push.outputs) }}
          ENGINE: ${{ matrix.engine }}
        run: |
          DIGEST=$(jq -er --arg e "$ENGINE" '.["digest-" + $e]' <<< "$OUTPUTS")
          echo "value=$DIGEST" >> "$GITHUB_OUTPUT"

      - name: Sign Docker image
        env:
          DIGEST: ${{ steps.digest.outputs.value }}
          REPO: ${{ github.repository }}
        run: |
          cosign sign --yes --new-bundle-format ghcr.io/${REPO}@${DIGEST}

      - name: Attest build provenance
        id: attest
        uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4.2.2
        with:
          subject-name: ghcr.io/${{ github.repository }}
          subject-digest: ${{ steps.digest.outputs.value }}
          push-to-registry: true

      - name: Read bundle content
        id: bundle
        env:
          BUNDLE: ${{ steps.attest.outputs.bundle-path }}
          ENGINE: ${{ matrix.engine }}
        run: |
          echo "content-${ENGINE}<<EOF" >> "$GITHUB_OUTPUT"
          cat "$BUNDLE" >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"

  upload-release-assets:
    needs: [build-and-push, sign-and-attest]
    runs-on: ubuntu-slim
    permissions:
      contents: write

    steps:
      - name: Check release
        id: check
        env:
          TAG: ${{ needs.build-and-push.outputs.tag }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          RELEASE=$(gh release view "$TAG" --repo "$GITHUB_REPOSITORY" --json isDraft 2>&1) || {
            echo "upload=false" >> "$GITHUB_OUTPUT"
            exit 0
          }
          if [ "$(echo "$RELEASE" | jq -r '.isDraft')" != "true" ]; then
            echo "::warning::Release $TAG is already published, skipping upload"
            echo "upload=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          echo "upload=true" >> "$GITHUB_OUTPUT"

      - name: Upload to Release
        if: steps.check.outputs.upload == 'true'
        env:
          TAG: ${{ needs.build-and-push.outputs.tag }}
          BUNDLE_UNIVERSAL: ${{ needs.sign-and-attest.outputs['bundle-content-universal'] }}
          BUNDLE_INSPECT: ${{ needs.sign-and-attest.outputs['bundle-content-inspect'] }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "$BUNDLE_UNIVERSAL" > isolated-run-container.sigstore.json
          echo "$BUNDLE_INSPECT" > isolated-run-container-inspect.sigstore.json
          gh release upload --repo "$GITHUB_REPOSITORY" "$TAG" \
            isolated-run-container.sigstore.json \
            isolated-run-container-inspect.sigstore.json
